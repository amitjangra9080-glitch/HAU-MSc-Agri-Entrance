# Server-only Firebase Admin credential.
# Paste the complete service-account JSON only in Vercel Environment Variables,
# never in this file and never in client-side JavaScript.
FIREBASE_SERVICE_ACCOUNT_JSON=
