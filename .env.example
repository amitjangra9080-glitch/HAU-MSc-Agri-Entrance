# Server-only Firebase Admin credential. Never add real secrets to this file.
# Recommended Vercel variables:
FIREBASE_PROJECT_ID=
FIREBASE_CLIENT_EMAIL=
FIREBASE_PRIVATE_KEY=

# Optional alternative: complete service-account JSON.
# If both formats exist, valid split variables are used as a fallback.
FIREBASE_SERVICE_ACCOUNT_JSON=
